"use strict";

var helpers = require("../../node_modules/@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");

var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
helpers.prelude(module);

try {
  Object.defineProperty(exports, "__esModule", {
    value: true
  });
  exports.isNum = isNum;
  exports.isBool = isBool;
  exports.isStr = isStr;
  exports.isFn = isFn;
  exports.parseClassName = parseClassName;
  exports.objectValues = objectValues;
  exports.hasToastId = hasToastId;
  exports.getAutoCloseDelay = getAutoCloseDelay;
  exports.canBeRendered = canBeRendered;
  exports.canUseDom = void 0;

  var _react = require("react");

  function isNum(v) {
    return typeof v === 'number' && !isNaN(v);
  }

  function isBool(v) {
    return typeof v === 'boolean';
  }

  function isStr(v) {
    return typeof v === 'string';
  }

  function isFn(v) {
    return typeof v === 'function';
  }

  function parseClassName(v) {
    return isStr(v) || isFn(v) ? v : null;
  }

  function objectValues(obj) {
    return Object.keys(obj).map(key => obj[key]);
  }

  function hasToastId(toastId) {
    return toastId === 0 || toastId;
  }

  function getAutoCloseDelay(toastAutoClose, containerAutoClose) {
    return toastAutoClose === false || isNum(toastAutoClose) && toastAutoClose > 0 ? toastAutoClose : containerAutoClose;
  }

  const canUseDom = !!(typeof window !== 'undefined' && window.document && window.document.createElement);
  exports.canUseDom = canUseDom;

  function canBeRendered(content) {
    return /*#__PURE__*/(0, _react.isValidElement)(content) || isStr(content) || isFn(content) || isNum(content);
  }

  helpers.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}